.claude/
review.html
.DS_Store
Thumbs.db

# ── 기출 원문(평문) — 절대 커밋하지 말 것 ─────────────────────
# 배포는 bank.enc / enc/*.enc 로 암호화해서만 한다.
# ⚠️ git 은 패턴 뒤 같은 줄의 주석을 인식하지 못한다. 주석은 반드시 따로 줄에.

# 기출 회차 문항 (CBT 복원문제) — data/20NN-N.js
data/20[0-9][0-9]-[0-9].js

# 기출 문제·풀이 그림
images/

# data/theory·concepts·lessons·calcs·bank-500 은 직접 만든 것이라 커밋한다.

__pycache__/
